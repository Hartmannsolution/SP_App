const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  tailwindConfig: './tailwind.config.js',
  formatOnSave: true,
  formatOnPaste: true,
  singleQuote: true,
  printWidth: 120,
};

export default config;
